all:
  hosts:
    staging:
      ansible_host: 57.130.10.246
      ansible_user: ubuntu
      ansible_ssh_private_key_file: "{{ lookup('env', 'SSH_KEY_PATH') | default('/tmp/deploy-key', true) }}"
  vars:
    dd_env: staging
    agent_memory: 8G
    agent_cpus: 4
    agent_node_size: standard
